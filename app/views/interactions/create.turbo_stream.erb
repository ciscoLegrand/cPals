<%= turbo_stream.replace "#{dom_id(@interaction.conversation)}_interactions" do %>
  <%= render "interaction", interaction: @interaction, scroll_to: true %>
<% end %>
<%= turbo_stream.replace "#{dom_id(@interaction.conversation)}_interaction_form" do %>
  <%= render "form", conversation: @interaction.conversation %>
<% end %>
